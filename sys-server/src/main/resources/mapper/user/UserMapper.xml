<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.casic.sys.mapper.UserMapper">

	<insert id="inster" parameterType="com.casic.sys.entity.UserEntity">
        insert into sys_user (id,user_name,user_sex,password,create_date,create_user_id,modify_date,modify_user_id,delete_state)
        values (#{id,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{userSex,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR},#{creaateDate,jdbcType=VARCHAR},#{createUserId,jdbcType=VARCHAR},#{modifyDate,jdbcType=VARCHAR},#{modifyUserId,jdbcType=VARCHAR},#{deleteState,jdbcType=VARCHAR})
    </insert>

    <select id="selectUser" resultType="com.casic.sys.entity.UserEntity" >
        select id,user_name,user_sex,password
        from sys_user
        <where>
            id= #{id,jdbcType=VARCHAR}
        </where>
    </select>
</mapper>