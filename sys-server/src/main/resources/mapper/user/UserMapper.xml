<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.casic.sys.mapper.UserMapper">

	<insert id="insterUser" parameterType="com.casic.sys.entity.UserEntity">
        insert into sys_user (id,user_name,user_sex,password)
        values (#{id,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{userSex,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR})
    </insert>

    <select id="selectUser" resultType="com.casic.sys.entity.UserEntity" >
        select id,user_name,user_sex,password
        from sys_user
        <where>
            id= #{id,jdbcType=VARCHAR}
        </where>
    </select>
</mapper>